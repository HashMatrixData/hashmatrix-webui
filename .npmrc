# ProComponents 仅声明 antd ^4 || ^5 peer，但本平台技术栈选定 antd v6（见 docs/00-前端初始化-spec.md）。
# 通过 pnpm overrides 强制全仓 antd 收敛到 v6，并放宽严格 peer 校验以避免安装期硬失败。
# 运行期兼容性由 typecheck + Storybook E2E 把关。
strict-peer-dependencies=false
auto-install-peers=true
# 信创：运行期资源自托管，构建期依赖走内网镜像时只需改此处 registry。
# registry=https://registry.npmjs.org/
